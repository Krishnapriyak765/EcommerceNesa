class Constants {
  //.env file path
  static const String envPath = "assets/.env";

  static const String baseURL = "baseURL";

  static double height = 0;
  static double width = 0;
  //width break points
  static double desktopBreakPoint = width < 1450 ? 1300 : 1400;
}
